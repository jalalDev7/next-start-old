import NewReservationClient from "@/components/dashboard/reservation/NewReservationClient";
import React from "react";

const page = () => {
  return <NewReservationClient />;
};

export default page;
